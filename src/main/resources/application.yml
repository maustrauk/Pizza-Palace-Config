server:
  port: 8888
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          clone-on-start: true
          default-label: main
          uri: https://github.com/maustrauk/Pizza-Palace-Config-Service